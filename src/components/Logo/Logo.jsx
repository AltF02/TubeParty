import React from 'react';
import './Logo.scss';

const Logo = () => (
    <div className="logoContainer">
        <h1>
            <span className="primaryColor">TUBE</span>
            <span className="secondaryColor">PARTY</span>
        </h1>
    </div>
);

export default Logo;
